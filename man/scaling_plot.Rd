% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{scaling_plot}
\alias{scaling_plot}
\title{Plot scaling factors (msr) across samples}
\usage{
scaling_plot(
  data = NULL,
  epk = NULL,
  stats_summary = NULL,
  condition = NULL,
  marker_levels = NULL,
  markers_to_exclude = c("INPUT"),
  legend_position = c("bottom", "right", "left", "top", "none"),
  save_plots = FALSE,
  save_dir = "",
  ncol = 3,
  sample_labeling = c("sample_id_rep", "sample_id", "map_id"),
  facet_scales = c("free_y", "fixed", "free_x", "free")
)
}
\arguments{
\item{data}{Backward-compatible alias for \code{stats_summary}. A data frame
containing at least \code{marker}, \code{sample_id_rep}, and \code{msr}.}

\item{epk}{Optional EPK object. If supplied,\code{epk$tables$stats_summary}
is used as plotting input.}

\item{stats_summary}{Optional data frame containing scaling information. If
provided, it takes precedence over \code{epk} and \code{data}.}

\item{condition}{Character vector specifying which conditions to plot.
\itemize{
  \item \code{NULL}: plot all available conditions as-is (default)
  \item \code{"All"}: collapse all rows into a single condition named \code{"All"}
  \item otherwise: one or more condition names to subset
}}

\item{marker_levels}{Optional character vector defining marker order for
facet display/fill legend. If \code{NULL}, uses order in data.}

\item{markers_to_exclude}{Character vector of markers to remove before
plotting. Default: \code{"INPUT"}.}

\item{legend_position}{Character string passed to
\code{theme(legend.position = ...)}.}

\item{save_plots}{Logical; if \code{TRUE}, save PNG files.}

\item{save_dir}{Character path to directory where plots are saved when
\code{save_plots = TRUE}.}

\item{ncol}{Integer; number of facet columns.}

\item{sample_labeling}{Character; column used for x-axis labels. One of
\code{"sample_id_rep"}, \code{"sample_id"}, or \code{"map_id"}.}

\item{facet_scales}{Character; passed to the \code{scales} argument of
\code{\link[ggplot2]{facet_wrap}}. One of \code{"free_y"} (default),
\code{"fixed"}, \code{"free_x"}, or \code{"free"}.
Use \code{"fixed"} to share a common y-axis range across all marker
facets, with the upper limit driven by the largest value in any marker.}
}
\value{
If one condition is selected, returns one ggplot object. If multiple
  conditions are selected, returns a named list of ggplot objects.
}
\description{
Creates bar plots of scaling factors (\code{msr}) across samples, faceted by
marker. This function follows the same input framework as
\code{plot_qc_stats()} and supports \code{epk}, \code{stats_summary}, or
legacy \code{data} input.
}
\examples{
\dontrun{
# Default: each facet has its own y-axis
p <- scaling_plot(
  epk = epk,
  condition = "All",
  legend_position = "none",
  ncol = 3,
  sample_labeling = "sample_id_rep"
)

# Fixed y-axis across all markers
p <- scaling_plot(
  epk = epk,
  condition = "All",
  facet_scales = "fixed"
)
p
}

}
